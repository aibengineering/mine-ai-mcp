name: copper-slope
description: Gather a useful raw-copper batch from a broad exposed vein on a stepped stone slope.

minecraft:
  version: "1.21.4"

world:
  type: flat
  time: noon
  gamerules:
    doDaylightCycle: false
    doMobSpawning: false
    keepInventory: true
    randomTickSpeed: 0

reset:
  - "fill -16 -59 -16 20 -40 16 air"

geometry:
  - fill: { block: granite, from: [-12, -60, -12], to: [18, -60, 12] }
  - fill: { block: stone, from: [5, -59, -5], to: [13, -54, 5] }
  - fill: { block: copper_ore, from: [5, -58, -2], to: [5, -57, 2] }
  - setblock: { block: copper_ore, at: [6, -58, 3] }

players:
  - name: Collector
    pos: [0, -59, 0]
    inventory:
      - { item: stone_pickaxe, count: 1 }

client:
  command: bun
  args: ["../../src/client-host.ts", "../../src/collector.ts"]

# The request this world exists to exercise.
params:
  block_name: copper_ore
  count: 8
  scaffold: false

tick:
  - "gamemode spectator @a[name=!Collector,gamemode=!spectator]"

goal:
  kind: all
  timeout: 45
  goals:
    - kind: hasItem
      who: Collector
      item: raw_copper
      count: 8
    - kind: completion
      who: Collector
